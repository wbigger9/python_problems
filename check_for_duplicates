from collections import Counter
from collections import defaultdict


string_name = "dog"

# Iterate over the string
def has_duplicates(a):
    orignal = list(a)
    print(orignal)
    count = list(Counter(a))
    print(count)
    if len(orignal) > len(count):
        return True
    else:
        return False

print(has_duplicates(string_name))


